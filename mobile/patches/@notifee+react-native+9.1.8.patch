diff --git a/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore+UNUserNotificationCenter.m b/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore+UNUserNotificationCenter.m
index beaa360..773f73d 100644
--- a/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore+UNUserNotificationCenter.m
+++ b/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore+UNUserNotificationCenter.m
@@ -125,6 +125,8 @@ - (void)userNotificationCenter:(UNUserNotificationCenter *)center
     }
 
     // if list or banner is true, ignore alert property
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
     if (banner || list) {
       if (banner) {
         if (@available(iOS 14, *)) {
@@ -147,6 +149,7 @@ - (void)userNotificationCenter:(UNUserNotificationCenter *)center
       // TODO: remove alert once it has been fully removed from the notifee API
       presentationOptions |= UNNotificationPresentationOptionAlert;
     }
+#pragma clang diagnostic pop
 
     NSDictionary *notifeeTrigger = notification.request.content.userInfo[kNotifeeUserInfoTrigger];
     if (notifeeTrigger != nil) {
diff --git a/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore.m b/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore.m
index 8f308dd..6f2001c 100644
--- a/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore.m
+++ b/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCore.m
@@ -423,17 +423,20 @@ + (UNMutableNotificationContent *)buildNotificationContent:(NSDictionary *)notif
     content.threadIdentifier = iosDict[@"threadId"];
   }
 
+  #pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
   if (@available(iOS 12.0, *)) {
-    // summaryArgument
+    // summaryArgument (deprecated in iOS 15, but needed for iOS 12-14)
     if (iosDict[@"summaryArgument"] != nil) {
       content.summaryArgument = iosDict[@"summaryArgument"];
     }
 
-    // summaryArgumentCount
+    // summaryArgumentCount (deprecated in iOS 15, but needed for iOS 12-14)
     if (iosDict[@"summaryArgumentCount"] != nil) {
       content.summaryArgumentCount = [iosDict[@"summaryArgumentCount"] unsignedIntValue];
     }
   }
+#pragma clang diagnostic pop
 
   if (@available(iOS 13.0, *)) {
     // targetContentId
@@ -488,12 +491,15 @@ + (void)getNotificationCategories:(notifeeMethodNSArrayBlock)block {
         }
       }
 
+      #pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
       if (@available(iOS 13.0, *)) {
         categoryDictionary[@"allowAnnouncement"] = @(
             ((notificationCategory.options & UNNotificationCategoryOptionAllowAnnouncement) != 0));
       } else {
         categoryDictionary[@"allowAnnouncement"] = @(NO);
       }
+#pragma clang diagnostic pop
 
       categoryDictionary[@"actions"] =
           [NotifeeCoreUtil notificationActionsToDictionaryArray:notificationCategory.actions];
@@ -546,11 +552,14 @@ + (void)setNotificationCategories:(NSArray<NSDictionary *> *)categories
       }
     }
 
+    #pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
     if (@available(iOS 13.0, *)) {
       if ([categoryDictionary[@"allowAnnouncement"] isEqual:@(YES)]) {
         options |= UNNotificationCategoryOptionAllowAnnouncement;
       }
     }
+#pragma clang diagnostic pop
 
     if (@available(iOS 12.0, *)) {
       category = [UNNotificationCategory categoryWithIdentifier:id
@@ -617,11 +626,14 @@ + (void)requestPermission:(NSDictionary *)permissions
     }
   }
 
+  #pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
   if ([permissions[@"announcement"] isEqual:@(YES)]) {
     if (@available(iOS 13.0, *)) {
       options |= UNAuthorizationOptionAnnouncement;
     }
   }
+#pragma clang diagnostic pop
 
   if ([permissions[@"carPlay"] isEqual:@(YES)]) {
     options |= UNAuthorizationOptionCarPlay;
diff --git a/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCoreUtil.m b/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCoreUtil.m
index a42f938..36901e7 100644
--- a/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCoreUtil.m
+++ b/node_modules/@notifee/react-native/ios/NotifeeCore/NotifeeCoreUtil.m
@@ -286,8 +286,9 @@ + (NSDictionary *)attachmentOptionsFromDictionary:(NSDictionary *)optionsDict {
     NSNumber *height = area[@"height"];
     CGRect areaRect =
         CGRectMake([x doubleValue], [y doubleValue], [width doubleValue], [height doubleValue]);
+    CFDictionaryRef rectDict = CGRectCreateDictionaryRepresentation(areaRect);
     options[UNNotificationAttachmentOptionsThumbnailClippingRectKey] =
-        (__bridge id _Nullable)(CGRectCreateDictionaryRepresentation(areaRect));
+        (__bridge_transfer id _Nullable)(rectDict);
   }
 
   if (optionsDict[@"thumbnailTime"] != nil) {
@@ -403,6 +404,8 @@ + (UNNotificationTrigger *)intervalTriggerFromDictionary:(NSDictionary *)trigger
   return [UNTimeIntervalNotificationTrigger triggerWithTimeInterval:intervalInSeconds repeats:true];
 }
 
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
 + (NSMutableArray<NSNumber *> *)intentIdentifiersFromStringArray:
     (NSArray<NSString *> *)identifiers {
   NSMutableArray<NSNumber *> *intentIdentifiers = [[NSMutableArray alloc] init];
@@ -488,7 +491,10 @@ + (UNNotificationTrigger *)intervalTriggerFromDictionary:(NSDictionary *)trigger
 
   return intentIdentifiers;
 }
+#pragma clang diagnostic pop
 
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
 + (NSMutableArray<NSString *> *)intentIdentifiersFromNumberArray:
     (NSArray<NSNumber *> *)identifiers {
   NSMutableArray<NSString *> *intentIdentifiers = [[NSMutableArray alloc] init];
@@ -574,6 +580,7 @@ + (UNNotificationTrigger *)intervalTriggerFromDictionary:(NSDictionary *)trigger
 
   return intentIdentifiers;
 }
+#pragma clang diagnostic pop
 
 /**
  * Returns timestamp in millisecons
